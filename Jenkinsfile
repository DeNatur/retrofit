pipeline {
    agent any
    stages {
        stage("build") {:
            steps {
                sh 'echo "Starting Building Project"'
            }
        }

        stage("test") {
            steps {
            }
    }
}